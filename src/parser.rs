use entry::Entry;

pub struct Parser;

impl Parser {
    pub fn bib_from_str(input: &str) -> Vec<Entry> {
        unimplemented!();
    }
    pub fn entry_from_str(entry_string: &str) -> Entry {
        unimplemented!();
    }
}